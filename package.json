{
    "scripts": {
        "git-synch": "git fetch && git pull && cd poefree-client && npm run format && cd ../poefree-server && npm run format && cd .. && git add . && git commit -am 'auto-committing' && git push"
    }
}
