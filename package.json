{
    "scripts": {
        "git-synch": "git fetch && git pull && cd poefree-client && npm run format && cd ../poefree-server && npm run format && cd .. && git add . && git commit -am \"git synch\" && git push && docker-compose down",
        "poefree": "docker-compose down && docker-compose up --build"
    }
}
